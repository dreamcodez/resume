#!/bin/bash -xeu

now_millis(){  python -c 'import time; print(int(time.time() * 1000))'; }

CONTAINER_TAG=$(echo -n "$CODEBUILD_SOURCE_VERSION" | head -c8)
CONTAINER_IMAGE="$CONTAINER_REPO:$CONTAINER_TAG"

deploy/bin/credstash-get-deploy-secrets
